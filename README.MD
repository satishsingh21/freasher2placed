# Install ugligy globally
npm install uglify-js -g

# To uglify code
uglifyjs script/script.js -o js/ugli.js -c -m

# Install reload globally
npm install  reload -g

# To refresh browser on code change
run command in terminal
reload -b